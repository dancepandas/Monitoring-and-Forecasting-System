# 后端网关依赖（Python 3.14.2 本机验证通过；Python 3.10+ 应均可）
# 安装：uv pip install -r requirements.txt

# ── floodmind（Agent 运行时，按 commit 钉死）──
floodmind @ git+https://github.com/dancepandas/FloodMind.git@f46e324125b0e0c34c8998aff1ec58a7f89c2353

# ── Web 框架 ──
fastapi==0.128.0
uvicorn==0.40.0          # Linux 服务器可改 uvicorn[standard]==0.40.0（含 uvloop/httptools，性能更佳）
python-multipart==0.0.32
pydantic==2.12.5
pydantic-settings==2.14.1
sqlalchemy==2.0.50

# ── 调度 / HTTP / 认证 ──
apscheduler==3.11.2
httpx==0.28.1
requests==2.32.5
bcrypt==5.0.0
PyJWT==2.13.0

# ── 报告 docx / 数据处理 ──
python-docx==1.2.0
markdown==3.10.2
numpy==2.4.1
pandas==3.0.1

# ── LLM / 预报 ──
dashscope==1.26.0
chronos-forecasting==2.2.2
torch==2.10.0            # 默认 CPU；GPU 服务器用 --index-url https://download.pytorch.org/whl/cu121 装对应 CUDA 版
